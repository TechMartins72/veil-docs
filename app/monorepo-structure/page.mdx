import { FileTree } from "nextra/components";

<br />

# Monorepo Structure

<FileTree>
  <FileTree.Folder name="veil-credit-scoring-protocol" defaultOpen>
    <FileTree.Folder name="packages" defaultOpen>
      <FileTree.Folder
        name="contract"
        label="contract — Compact smart contract, witnesses, and tests"
        defaultOpen
      >
        <FileTree.Folder name="src" defaultOpen>
          <FileTree.File
            name="main.compact"
            label="main.compact — Contract entry point, exports all circuits"
          />
          <FileTree.File
            name="bootstrap.compact"
            label="bootstrap.compact — Lightweight bootstrap contract for staged deployment"
          />
          <FileTree.Folder name="modules" defaultOpen>
            <FileTree.File
              name="Admin.compact"
              label="Admin.compact — Admin management circuits"
            />
            <FileTree.File
              name="CustomStructs.compact"
              label="CustomStructs.compact — All struct/enum type definitions"
            />
            <FileTree.File
              name="GlobalLedgerStates.compact"
              label="GlobalLedgerStates.compact — All on-chain ledger state declarations"
            />
            <FileTree.File
              name="PoTNFT.compact"
              label="PoTNFT.compact — NFT mint / renew / verify circuits"
            />
            <FileTree.File
              name="Scoring.compact"
              label="Scoring.compact — Score entry and event submission circuits"
            />
            <FileTree.File
              name="Utils.compact"
              label="Utils.compact — Shared utilities, witness declarations, score recompute"
            />
          </FileTree.Folder>
          <FileTree.Folder
            name="tests"
            label="tests — Vitest integration tests"
          />
          <FileTree.Folder
            name="managed"
            label="managed — Compact compiler output (generated, do not edit)"
          />
          <FileTree.File
            name="witness.ts"
            label="witness.ts — TypeScript witness implementations and private state types"
          />
        </FileTree.Folder>
        <FileTree.Folder
          name="dist"
          label="dist — Built contract artifacts (generated by bun run build)"
        />
      </FileTree.Folder>
      <FileTree.Folder
        name="cli"
        label="cli — Interactive CLI DApp for local and remote environments"
        defaultOpen
      >
        <FileTree.Folder name="src" defaultOpen>
          <FileTree.File
            name="index.ts"
            label="index.ts — Main menu loop and contract API calls"
          />
          <FileTree.File
            name="config.ts"
            label="config.ts — Environment configs (standalone, preview, preprod)"
          />
          <FileTree.File
            name="midnight-wallet-provider.ts"
            label="midnight-wallet-provider.ts — Wallet integration"
          />
          <FileTree.File
            name="generate-dust.ts"
            label="generate-dust.ts — Dust token generation helper"
          />
          <FileTree.Folder name="launcher" defaultOpen>
            <FileTree.File
              name="standalone.ts"
              label="standalone.ts — Local Docker-based launcher"
            />
            <FileTree.File
              name="preview.ts"
              label="preview.ts — Midnight Preview network launcher"
            />
            <FileTree.File
              name="preprod.ts"
              label="preprod.ts — Midnight Preprod network launcher"
            />
          </FileTree.Folder>
        </FileTree.Folder>
        <FileTree.Folder
          name="logs"
          label="logs — Per-session log files (gitignored)"
        />
      </FileTree.Folder>
    </FileTree.Folder>
  </FileTree.Folder>
</FileTree>
